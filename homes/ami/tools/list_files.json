{"name":"list_files","description":"List files and directories at a given path. Returns each entry prefixed with [file] or [dir]. Use this when you want a directory listing without spawning a shell command.","input_schema":{"type":"object","properties":{"path":{"type":"string","description":"Directory path to list (absolute or ~ relative). Defaults to home directory if omitted."}},"required":[]}}