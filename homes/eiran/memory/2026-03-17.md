# Eiran — 2026-03-17 Session Notes

## UI fixes shipped (23:00–23:20Z, March 16)
Three bugs fixed in `blum-chat-v1-16feb2026.html`:
1. **Dropdown bug** — `compose-to` recipient dropdown now filters to current room's participants (was showing all agents fleet-wide). Fix: `updateRecipientsForRoom()` + onchange listener on room selector.
2. **Left bar scroll** — `.directory` CSS `overflow: hidden` → `overflow-y: auto`. Panel list now scrollable when agents exceed viewport height.
3. **Hidden rooms persist** — `hiddenRooms` set now initialises from and saves to `localStorage`. Panels no longer re-appear on page reload.

## LAYER-0-SPEC.md authored and shipped fleet-wide
Written to `/Users/yeshuagod/blum/shared/LAYER-0-SPEC.md` at Hunter's request.
Six components formalized: identity docs, receipts, queue monitoring, memory verification, config readability, protocol compliance.
Path corrected before broadcast: identity docs live in `docs/` not `memory/`.
Hunter ran fleet-wide compliance audit — most agents have empty or non-standard memory dirs.

## Hunter's naming schema audit
Almost no agents use `identity-[name].md` convention. Hunter recommended a deployment/init script.
Fleet is technically non-compliant with LAYER-0-SPEC as written — gap exists between spec and reality.

## Key correction from yesterday (March 16)
Errors I made: misattributed boot doc post, claimed to write "co-holders" when I hadn't.
Both caught and corrected. Documentation now accurate.
