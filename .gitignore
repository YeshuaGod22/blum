.DS_Store
node_modules/
**/data/
*.log

# Auth / secrets — never commit these
auth-profiles.json
openclaw.json
**/config.json
**/*.bak*
**/*.bak

# Nested duplicate directory
blum/

# Nested repos (not submodules — standalone repos cloned inside blum)
AutoResearchClaw/

# Runtime agent state — changes every cycle, not meaningful to track
**/homelogfull/
**/history/
**/projects/
**/tools/

# But the canonical shared tool set IS tracked
!shared/tools/
!shared/tools/*.json
**/internal/
**/blocked.json
**/rooms.json
**/inbox/*.md
home.log
ops.log
**/handoff-warning.md
